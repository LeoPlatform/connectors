'use strict';

// Checksum bot migration is deferred — this stub satisfies the connector interface.
// Implement when a checksum bot migration is requested.
module.exports = function() {
	throw new Error('checksum not implemented');
};
